1. run "pip install -r requirements.txt" to install all the requirements
2. run "python inference.py --image_path[image_path]" to get inference result from single image
3. run "python class_accuracy.py" to get accuracy for every class
4. run "python random_class_inference.py" to get 4 random example from each class 
